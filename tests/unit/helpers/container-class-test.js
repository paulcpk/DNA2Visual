
import { containerClass } from 'dna2visual/helpers/container-class';
import { module, test } from 'qunit';

module('Unit | Helper | container class');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = containerClass([42]);
  assert.ok(result);
});

